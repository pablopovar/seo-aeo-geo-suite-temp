DASHBOARD_PORT=4018
AEO_AUDITOR_URL=http://host.docker.internal:8000

# Optional. Leave blank until the existing GEO/AEO auditor's CLI/API contract
# is confirmed. Supports {domain} and {url}.
#
# Example:
# AEO_AUDIT_COMMAND=python /some/adapter.py --url {url}
AEO_AUDIT_COMMAND=
